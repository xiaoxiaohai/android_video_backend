# Option A: Dedicated static site (only if you DON'T already use nexamedia.app elsewhere)
#
# server {
#   listen 80;
#   server_name nexamedia.app www.nexamedia.app;
#
#   root /home/ubuntu/apps/nexa-legal-web;
#   index privacy.html;
#
#   location / {
#     try_files $uri $uri.html =404;
#   }
#
#   # Assets: basic caching (optional)
#   location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
#     expires 1h;
#     add_header Cache-Control "public, max-age=3600";
#   }
# }
#
# Option B: Add these locations into your EXISTING server block for nexamedia.app
#
# root /home/ubuntu/apps/nexa-legal-web;
# location = /privacy { try_files /privacy.html =404; }
# location = /terms { try_files /terms.html =404; }
# location /assets/ { try_files $uri =404; }

